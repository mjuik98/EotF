import { confirmPauseReturnToTitle } from '../../../title/application/help_pause_title_actions.js';

export function confirmReturnToTitleRuntime(deps = {}) {
  return confirmPauseReturnToTitle(deps);
}
