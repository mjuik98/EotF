import { playIntroCinematicRuntime } from '../../../../ui/title/intro_cinematic_runtime.js';

export const IntroCinematicUI = {
  play(deps = {}, onComplete) {
    playIntroCinematicRuntime(deps, onComplete);
  },
};
