'use strict';

export const STATUS_EFFECT_ASSET_DATA = Object.freeze({
  resonance: Object.freeze({ icon: '🎵', buff: true }),
  acceleration: Object.freeze({ icon: '⚡', buff: true }),
  mirror: Object.freeze({ icon: '🪞', buff: true }),
  time_warp: Object.freeze({ icon: '⏳', buff: true }),
  time_warp_plus: Object.freeze({ icon: '⌛', buff: true }),
  vanish: Object.freeze({ icon: '🌫', buff: true }),
  shadow_atk: Object.freeze({ icon: '🌒', buff: true }),
  dodge: Object.freeze({ icon: '💨', buff: true }),
  focus: Object.freeze({ icon: '🎯', buff: true }),
  critical_turn: Object.freeze({ icon: '💥', buff: true }),
  lifesteal: Object.freeze({ icon: '🩸', buff: true }),
  spike_shield: Object.freeze({ icon: '🌵', buff: true }),
  immune: Object.freeze({ icon: '🔰', buff: true }),
  soul_armor: Object.freeze({ icon: '🛡', buff: true }),
  zeroCost: Object.freeze({ icon: '0', buff: true }),
  strength: Object.freeze({ icon: '💪', buff: true }),
  dexterity: Object.freeze({ icon: '🤸', buff: true }),
  echo_on_hit: Object.freeze({ icon: '🎶', buff: true }),
  blessing_of_light: Object.freeze({ icon: '☀️', buff: true }),
  blessing_of_light_plus: Object.freeze({ icon: '🌞', buff: true }),
  divine_grace: Object.freeze({ icon: '🙏', buff: true }),
  divine_aura: Object.freeze({ icon: '😇', buff: true }),
  protection: Object.freeze({ icon: '🧱', buff: true }),
  endure_buff: Object.freeze({ icon: '🪨', buff: true }),
  berserk_mode: Object.freeze({ icon: '😡', buff: true }),
  berserk_mode_plus: Object.freeze({ icon: '🤬', buff: true }),
  echo_berserk: Object.freeze({ icon: '⚔️', buff: true }),
  unbreakable_wall: Object.freeze({ icon: '🧱', buff: true }),
  unbreakable_wall_plus: Object.freeze({ icon: '🏯', buff: true }),
  thorns: Object.freeze({ icon: '🌹', buff: true }),
  stunImmune: Object.freeze({ icon: '🛡️', buff: true }),
  weakened: Object.freeze({ icon: '🪶', buff: false }),
  slowed: Object.freeze({ icon: '🐢', buff: false }),
  burning: Object.freeze({ icon: '🔥', buff: false }),
  cursed: Object.freeze({ icon: '🕯', buff: false }),
  poisoned: Object.freeze({ icon: '☠', buff: false }),
  stunned: Object.freeze({ icon: '⏸', buff: false }),
  confusion: Object.freeze({ icon: '🌀', buff: false }),
  vulnerable: Object.freeze({ icon: '💔', buff: false }),
  marked: Object.freeze({ icon: '🎯', buff: false }),
  doom: Object.freeze({ icon: '💀', buff: false }),
});
