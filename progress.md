Original prompt: 우선순위대로 모두 진행해줘

- 2026-03-22: Start pass for relic detail follow-up work.
- Scope for this chunk: keyboard/accessibility on layout-only relic surfaces, formal detail panel variants, subtle panel transitions, and one more relic surface using layout details instead of tooltips.
- 2026-03-22: Added failing tests first for keyboard navigation, inline panel variant, and class-select relic detail layout. Implemented shared navigation + variant support, combat/stage keyboard navigation, and class-select shared relic detail panel.
- 2026-03-22: Verification pass: targeted suites and broader relic/layout suites passed, combat browser smoke passed, build passed. Import coupling lint remains red on this branch family (HEAD already fails at 51 feature->shared vs 44 baseline; current worktree is 55 because of intentional shared renderer reuse).

- 2026-03-22: Added echo-kill -> reward browser smoke coverage, shared item-detail surface state for stage/class-select, right-side combat log responsive tuning, and card CSS regression checks.
- 2026-03-22: Final verification passed: vitest 533/1323, lint, build + bundle budget, #mainStartBtn smoke, and combat-ui smoke with echo kill reward handoff.

- 2026-03-23: Debugged combat runtime regressions after tooltip/card CSS follow-up. Root cause for `잔향검사` passive was `ClassMechanics`/`SetBonusSystem` being owned by core progression modules but still resolved from combat scope in legacy compat paths, leaving browser combat deps without the class hook.
- 2026-03-23: Added failing tests first for core-scoped compat resolution and base-card tooltip injection, then aligned module resolution (`ClassMechanics`/`SetBonusSystem` -> core) and removed direct `window.showTooltip` fallback from hand card rendering in favor of explicit contract-provided handlers. Combat smoke now asserts `#cardTooltip.visible` on hand-card hover.
- 2026-03-23: Follow-up pass completed for remaining priorities. Added a runtime regression test for swordsman resonance damage growth, exposed `classMechanics` on legacy combat deps, switched legacy `TooltipUI` globals to combat-canonical resolution, and removed one dead combat card contract prop to pull `ui-combat` back under the bundle budget.
- 2026-03-23: Started next follow-up for remaining recommendations. Added failing guardrails first for keeping `HelpPauseUI` out of the static UI public barrel and for routing the shared combat `tooltip_ui.js` runtime into the tooltip chunk, then implemented the minimal surface split (`game/ui/screens/help_pause_ui.js` now points directly at the feature module while the public help/pause capability barrel exports helpers only).
- 2026-03-23: Refined the follow-up after boundary and chunking feedback. `HelpPauseUI` now uses a narrow feature public entrypoint (`game/features/ui/ports/public_help_pause_ui.js`) so compat stays on public surfaces without reintroducing the broad static barrel import, and combat copy now lives in a dedicated `ui-combat-copy` shared chunk so `ui-combat` regains budget headroom without the earlier tooltip-chunk cycle.

- 2026-03-23: Removed deprecated hover/player class-special HUD rendering paths from active HUD updates while keeping the updateClassSpecialUI contract as a no-op, and tightened combat smoke so the resonance post-attack text dump must still expose the resonance buff state.
- 2026-03-23: Follow-up verification found the combat smoke was still pinned to removed legacy surfaces (`window.FeedbackUI.showItemToast` and older card-clone keyword body selectors); updated the smoke + string guardrails to use the currently exposed `showItemToast` binding and the live docked keyword panel structure before rerunning build/budget/full smoke.

- 2026-03-23: Completed hand-card hover follow-up. Added multi-keyword docked panel, smarter avoid-rect placement, hover continuity across card/clone, and stronger card-panel linkage. Verification: targeted hover suites passed, build passed, character-select smoke passed, and combat smoke passed against a local dist server. Full npm test still has unrelated pre-existing red in feedback_ui_toasts/feedback_ui_toasts_css on this branch.
- 2026-03-23: Unified run hotkey blocking behind shared modal-state gates so pause/help/codex/settings/deck/full-map overlays all suppress gameplay hotkeys consistently across global and map-overlay handlers. Follow-up regression pass also brought the hand-card clone/card-frame suites green by removing legacy classic hand-card tooltip listeners from the clone-preview path, restoring exact hover-frame markup expectations, and binding clone keyword-panel open state/placement through runtime metadata. Fresh verification now passes end-to-end: targeted hotkey/hover suites, full `npm test` (538 files / 1376 tests), `npm run build`, and `npm exec -- node scripts/character_select_smoke_check.mjs` with `errors: []`.
- 2026-03-23: Follow-up improvements landed after the hotkey/card-hover fix. Added a DOM-derived `getRunHotkeyState()` helper so global run hotkeys and map overlay hotkeys share the same modal-state gate without adding a new GS input mode. Added browser smoke coverage in `scripts/help_pause_hotkey_smoke_check.mjs` for ESC subpanels (`도감`, `환경 설정`, `컨트롤 안내`) and verified `M`/`TAB` stay blocked while each surface is open. Extracted shared dataset-state helpers under `game/shared/ui/state/ui_state_dataset.js` and reused them for hover/overlay surfaces (item detail panels, card clone keyword panel, clone placement metadata) while documenting the clone-preview-only hover contract near the hand-card runtime. Fresh verification: focused suites green, full `npm test` now 540 files / 1382 tests, `npm run build` green, `npm exec -- node scripts/help_pause_hotkey_smoke_check.mjs` returned all blockers `true` with `errors: []`, and `npm exec -- node scripts/character_select_smoke_check.mjs` still returned `errors: []`.
- 2026-03-23: Completed the next hotkey/input-policy follow-up. Expanded `getRunHotkeyState()` into explicit `modal/cutscene/navigation/combat/title/exploration` modes, moved escape dismissal priority so active run surfaces close before pause, and tightened deck/full-map gating to honor non-playable states such as cutscenes. Extended `scripts/help_pause_hotkey_smoke_check.mjs` through a real combat transition so `도감` in combat now proves `Tab/Q/E/Enter/1` stay blocked and that the first `ESC` closes the active surface before the second `ESC` reopens pause. Also finished the hover/open-state cleanup by reusing shared dataset readers/writers and existing item-detail surface state APIs across clone runtime, run inscriptions, relic panels, class-select relic detail, and combat/reset paths. Verification: focused regression suites green, full `npm test` passed at 541 files / 1390 tests, `npm exec -- node scripts/help_pause_hotkey_smoke_check.mjs` returned all five booleans `true` with `errors: []`, `npm exec -- node scripts/character_select_smoke_check.mjs` returned `errors: []`, and the generated smoke screenshot looked correct. `npm run build` still fails on pre-existing bundle budget overages (`assets/index-*.css` and `assets/ui-shell-overlays-*.js`) rather than from a compile/runtime error.
- 2026-03-23: Recovered the bundle-budget follow-up without changing GS/input-state ownership. Moved `game/core/settings_manager.js` into the `ui-settings` chunk so `ui-shell-overlays` no longer owns settings persistence, and removed the duplicated run-settings layout block from `css/styles.css` so the feature-local `run-rules-redesign.css` stays the single source for that UI. Added explicit `RUN_HOTKEY_MODE_POLICY` / `getRunHotkeyPolicy()` coverage, kept `getRunHotkeyState()` DOM-derived, and pushed the shared surface state controller into run inscriptions, map relic detail, item detail, and card-clone keyword/open-state flows.
- 2026-03-23: Verification for the bundle/policy/controller follow-up: new RED-first guardrails passed (`tests/vite_chunking_guardrails.test.js`, `tests/help_pause_hotkey_policy_table.test.js`, `tests/ui_surface_state_controller.test.js`, `tests/ui_surface_controller_usage.test.js`, `tests/run_mode_style_split.test.js`), impacted runtime suites passed, `npm run build` passed with budgets green (`main entry css 192.96 / 193.00 kB`, `ui-shell-overlays 23.91 / 24.00 kB`), `npm exec -- node scripts/help_pause_hotkey_smoke_check.mjs` returned all blocker booleans `true` with `errors: []`, and `npm exec -- node scripts/character_select_smoke_check.mjs` returned `errors: []`. Full `npm test` is not green on this branch snapshot: unrelated pre-existing failures remain in `tests/character_select_modal.test.js` (`ECHO SKILL TREE` expectation drift) and `tests/player_facing_localization_regression.test.js` (`blessing_of_light.nextTurnText` copy mismatch).
